// Test highlighting for type-prefixed paths

// Example 1: Pointer type in path - T should be @type
fn ::*T::add(self: *T, other: *T) -> *T
    pass

// Example 2: Generic type in path - Array should be highlighted, T as @type
fn ::Array[T]::get(self: *Array[T], index: u32) -> T
    pass

// Example 3: Simple generic - Vec should be @module
fn ::Vec[T]::push(self: *mut Vec[T], item: T)
    pass

// Example 4: Regular namespaced path - something should be @module
fn something::add(x: i32, y: i32) -> i32
    pass

// Example 5: Multi-segment namespace - all intermediates should be @module
fn std::collections::HashMap::new[K,V]() -> HashMap[K,V]
    pass
